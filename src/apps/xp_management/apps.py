from django.apps import AppConfig


class XpManagementConfig(AppConfig):
    """Django app configuration for XP Management."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.xp_management"
    verbose_name = "XP Management"
